const answer = confirm("Are you sure?");
console.log(answer);

const answer2 = prompt("2 + 2 = ?");
if(answer2 == "4"){
    console.log("true");
}
let value = window.location;
value = window.outerHeight;//get window height

//window.location.reload(); refresh page

console.log(value);